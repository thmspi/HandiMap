extends layout

block content
  .search-container
    form(action='/search' method='get')
      input(type='text' name='query' placeholder='Search establishments...')
      button(type='submit') Search
